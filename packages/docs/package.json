{
	"name": "docs",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "vitepress dev --port 3000",
		"build": "vitepress build",
		"preview": "vitepress preview",
		"deploy:dev": "pnpm run build && wrangler pages deploy .vitepress/dist --project-name=vuetils-form-docs-dev --commit-dirty",
		"deploy:prod": "pnpm run build && wrangler pages deploy .vitepress/dist --project-name=vuetils-form-docs-prod --commit-dirty"
	},
	"dependencies": {
		"@vuetils/form": "workspace:latest"
	},
	"devDependencies": {
		"sass": "^1.64.2",
		"unocss": "^0.54.1",
		"vitepress": "1.0.0-beta.7",
		"wrangler": "^3.4.0"
	}
}
